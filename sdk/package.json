{
  "name": "@blockassist/sdk",
  "version": "0.1.0",
  "description": "BlockAssist SDK — shared types and matching primitives for mobile, web, and server.",
  "main": "dist/index.js",
  "module": "dist/index.mjs",
  "types": "dist/index.d.ts",
  "files": [
    "dist",
    "src"
  ],
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "test": "vitest run",
    "test:watch": "vitest",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@solana/web3.js": "1.95.3"
  },
  "devDependencies": {
    "@types/node": "^20.14.10",
    "typescript": "~5.3.3",
    "vitest": "^1.6.0"
  },
  "engines": {
    "node": ">=20"
  },
  "license": "UNLICENSED",
  "private": true
}

import { View, Text } from 'react-native';

export default function MarketplaceScreen() {
  return (
    <View style={{ flex: 1, padding: 20 }}>
      <Text style={{ fontSize: 24, fontWeight: 'bold' }}>Marketplace</Text>
      <Text>Fast Orders + Custom Jobs (v2.1)</Text>
    </View>
  );
}